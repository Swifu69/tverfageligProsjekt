<nav class="navbar navbar-expand-lg bg-dark navbar-dark">
  <div class="container">
    <a class="navbar-brand" href="/"
      ><img
        id="MDB-logo"
        src="images/passportjs.svg"
        alt="MDB Logo"
        draggable="false"
        height="30"
    /></a>
    <button
      class="navbar-toggler"
      type="button"
      data-mdb-toggle="collapse"
      data-mdb-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <i class="fas fa-bars"></i>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto align-items-center">
        <% if(typeof user != 'undefined') {%>
        <li class="nav-item">
          <a class="nav-link mx-2" href="/documents"><i class="fas fa-plus-circle pe-2"></i>Documents</a>
        </li>
        <li class="nav-item dropdown">
          <a
            class="nav-link dropdown-toggle btn btn-primary rounded-pill text-light"
            href="#"
            id="navbarDropdown"
            role="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            <%=user.username%>
          </a>
          <ul class="dropdown-menu bg-danger rounded-pill" aria-labelledby="navbarDropdown">
            <li><a class="dropdown-item bg-danger text-light d-block rounded-pill" href="/logout">Logout</a></li>
          </ul>
          </li>

        <% } %> 
        <% if(typeof user == 'undefined'){ %>
        <li class="nav-item ms-3">
          <a class="btn btn-primary rounded-pill" href="/register">Register</a>
        </li>
        <li class="nav-item ms-3">
          <a class="btn btn-danger rounded-pill" href="/login">Login</a>
        </li>
      <% } %> 
      </ul>
    </div>
  </div>
</nav>
<!-- Navbar -->